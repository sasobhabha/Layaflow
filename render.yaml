# Render Blueprint - single service, builds from root Dockerfile
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: layaflow
    runtime: docker
    plan: starter
    region: oregon
    healthCheckPath: /
    envVars:
      - key: PORT
        value: "8000"
